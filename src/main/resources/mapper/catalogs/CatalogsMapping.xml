<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.cilicili.frontend.catalogs.mapper.CatalogsMapper">
    
    <resultMap type="site.cilicili.frontend.catalogs.domain.pojo.CatalogsEntity" id="CatalogsMap">
        <result property="catalogId" column="catalog_id" jdbcType="INTEGER"/>
        <result property="courseId" column="course_id" jdbcType="INTEGER"/>
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
    </resultMap>
    
    <!--查询单个-->
    <select id="queryById" resultMap="CatalogsMap">
        select catalog_id,
               course_id,
               id,
               title,
               description
        from catalogs
        where catalog_id = #{catalogId}
    </select>
    
    <!--查询指定行数据-->
    <select id="queryAll" resultMap="CatalogsMap">
        select
        catalog_id, course_id, id, title, description
        from catalogs
        <where>
            <if test="catalogId != null">
                and catalog_id = #{catalogId}
            </if>
            <if test="courseId != null">
                and course_id = #{courseId}
            </if>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="description != null and description != ''">
                and description = #{description}
            </if>
        </where>
    </select>
    
    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(*)
        from catalogs
        <where>
            <if test="catalogId != null">
                and catalog_id = #{catalogId}
            </if>
            <if test="courseId != null">
                and course_id = #{courseId}
            </if>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="description != null and description != ''">
                and description = #{description}
            </if>
        </where>
    </select>
    
    <!--新增所有列-->
    <insert id="insert" keyProperty="catalogId" useGeneratedKeys="true">
        insert into catalogs(course_id, id, title, description)
        values (#{courseId}, #{id}, #{title}, #{description})
    </insert>
    
    <insert id="insertBatch" keyProperty="catalogId" useGeneratedKeys="true">
        insert into catalogs(course_id,id, title, description)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.courseId}, #{entity.id}, #{entity.title}, #{entity.description})
        </foreach>
    </insert>
    
    <insert id="insertOrUpdateBatch" keyProperty="catalogId" useGeneratedKeys="true">
        insert into catalogs(id, title, description)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.id}, #{entity.title}, #{entity.description})
        </foreach>
        on duplicate key update
        id = values(id),
        title = values(title),
        description = values(description)
    </insert>
    
    <!--通过主键修改数据-->
    <update id="update">
        update catalogs
        <set>
            <if test="id != null">
                id = #{id},
            </if>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
        </set>
        where catalog_id = #{catalogId}
    </update>
    
    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from catalogs
        where catalog_id = #{catalogId}
    </delete>


</mapper>


